name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: install conan
      run: pip3 install conan
    - name: build package
      run: ./build.sh
